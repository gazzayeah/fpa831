"""
Accounts-payable assistant package.

This package is the Python implementation of the take-home workflow:
gather policy and master-data evidence, reconcile in code, recommend an
outcome, then pause for human approval before any posting.

Import ``Workflow`` for CLI and eval. ``root_agent`` is an optional ADK
wrapper and is None when google-adk is not installed.
"""
